/*
============================================================
MONGODB - TOPIC 53: QUERY OPERATORS
============================================================

Comparison operators:

$eq   Equal
$ne   Not equal
$gt   Greater than
$gte  Greater than or equal
$lt   Less than
$lte  Less than or equal
$in   Matches any value in an array
$nin  Matches none of the values

Logical operators:

$and
$or
$not
$nor

============================================================
*/

use companyDB

// ----------------------------------------------------------
// $GT
// ----------------------------------------------------------

db.employees.find({
    salary: { $gt: 70000 }
})

// ----------------------------------------------------------
// $GTE
// ----------------------------------------------------------

db.employees.find({
    salary: { $gte: 80000 }
})

// ----------------------------------------------------------
// $LT
// ----------------------------------------------------------

db.employees.find({
    salary: { $lt: 70000 }
})

// ----------------------------------------------------------
// $LTE
// ----------------------------------------------------------

db.employees.find({
    salary: { $lte: 60000 }
})

// ----------------------------------------------------------
// $NE
// ----------------------------------------------------------

db.employees.find({
    department: { $ne: "HR" }
})

// ----------------------------------------------------------
// $IN
// ----------------------------------------------------------

db.employees.find({
    department: {
        $in: ["Engineering", "Finance"]
    }
})

// ----------------------------------------------------------
// $AND
// ----------------------------------------------------------

db.employees.find({
    $and: [
        { department: "Engineering" },
        { salary: { $gte: 80000 } }
    ]
})

// ----------------------------------------------------------
// $OR
// ----------------------------------------------------------

db.employees.find({
    $or: [
        { department: "Finance" },
        { salary: { $gt: 85000 } }
    ]
})
